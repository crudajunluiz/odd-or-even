# Even or Odd Detector

This Python program determines if a given integer is even or odd using the modulo operator.

## How to Run
1. Enter an integer when prompted.
2. The program will output whether the number is even or odd.
